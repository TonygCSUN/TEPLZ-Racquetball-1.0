//
//  ViewController.swift
//  TEPLZ Racquetball - 1.0
//
//  Created by TonyGuerrero on 2/26/15.
//  Copyright (c) 2015 Apps for TEPLZ. All rights reserved.
//

import UIKit

class AdvancedLessonsViewController: UIViewController {

    
    
    /*
    @IBAction func basicShotsButton(segue: UIStoryboardSegue)
    {
        let basicShotsTableViewController = segue.sourceViewController as BasicShotsTableViewController
    }
    */
    
    /*
    @IBAction func basicShotsButton(sender: AnyObject) {
    }
        self.performSegueWithIdentifier("push", sender: self)
    
        override func prepareForSegue(segue: showBasicShots, sender: AnyObject?) {
        // Get the new view controller using segue.destinationViewController.
            
            
        // var secondScene = segue.destinationViewController as BasicShotsViewController
        // Pass the selected object to the new view controller.
    }
    */
    
    
 
  
    
  
    
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepareForSegue(segue: UIStoryboardSegue, sender: AnyObject?) {
        // Get the new view controller using segue.destinationViewController.
        // Pass the selected object to the new view controller.
    }
    */

}
